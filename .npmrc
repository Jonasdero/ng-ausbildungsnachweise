# @angular/fire and angular-eslint trail the latest Angular release by a major
# version, so their peerDependencies do not yet list Angular 22 even though they
# work fine with it. Enabling legacy-peer-deps lets `npm install` resolve the
# tree without manual flags. Remove this once those packages ship Angular 22
# compatible peer ranges.
legacy-peer-deps=true
